export const SCHEDULE_GRID_MS = 6000;

// dataTransfer MIME type used when dragging a single media item from a picker
// rail onto the schedule timeline. The payload is the media key (mediaId).
export const MEDIA_DRAG_MIME = "application/x-linearcast-media";

// dataTransfer MIME type used when dragging a batch picker item onto the
// schedule timeline. The payload is JSON describing a group/show/album/artist.
export const SCHEDULE_BATCH_DRAG_MIME = "application/x-linearcast-schedule-batch";
